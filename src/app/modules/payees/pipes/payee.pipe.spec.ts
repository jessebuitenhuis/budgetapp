/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PayeePipe } from './payee.pipe';

describe('Pipe: Payeee', () => {
  it('create an instance', () => {
    let pipe = new PayeePipe();
    expect(pipe).toBeTruthy();
  });
});
